import { LbarDirective } from './lbar.directive';

describe('LbarDirective', () => {
  it('should create an instance', () => {
    const directive = new LbarDirective();
    expect(directive).toBeTruthy();
  });
});
